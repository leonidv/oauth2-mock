name: Publish new version

on: [push]

jobs:
    build:
        uses: ./.github/workflows/build.yaml
    publish:
        runs-on: ubuntu-latest
        name: Publish  to docker
        steps:
          - name: Fetching the application
            uses: actions/download-artifact@v7
            with:
                name: application-binary
          - run: ls -R


    


